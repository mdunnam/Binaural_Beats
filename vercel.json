{
  "buildCommand": "cd app && npm install && npx vite build",
  "outputDirectory": "app/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/((?!assets|sounds|music|vite\\.svg).*)", "destination": "/index.html" }
  ]
}
