{
  "buildCommand": "cd app && npm install && npx vite build && cd .. && cp -r landing/. dist/ && mkdir -p dist/app && cp -r app/dist/. dist/app/",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/app/(.*)", "destination": "/app/index.html" },
    { "source": "/",         "destination": "/index.html" }
  ]
}
